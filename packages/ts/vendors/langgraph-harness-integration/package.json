{
  "name": "@harness/langgraph-harness-integration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reference integration: a langgraph-harness config pack (commands.harnessIngest atom + ingest→classify flow) that extends the vendored langgraph-harness platform with @harness/ CSV/XLSX ingestion. Mock-first, offline.",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "flow": "./bin/flow.mjs"
  },
  "scripts": {
    "test": "node --test",
    "verify": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@clack/core": "1.4.3",
    "@clack/prompts": "1.7.0",
    "@harness/langgraph-repo-remediation-html-report-generator": "0.0.0",
    "@harness/sdk": "0.0.0",
    "chalk": "5.6.2"
  }
}
