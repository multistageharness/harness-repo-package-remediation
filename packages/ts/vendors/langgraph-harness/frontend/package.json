{
  "name": "@internal/langgraph-langchain-harness-frontend",
  "version": "100.0.0",
  "type": "module",
  "description": "langgraph-langchain-harness frontend — Vite vanilla-JS console: flow catalog, graph topology viewer, run panel with live SSE event stream, pattern registry browser.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pretest": "vite build",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "8.0.16"
  }
}
